services:
  mongo:
    networks:
      - backend
  
  core:
    networks:
      - pangolin
      - backend

  periphery:
    networks
      - backend

networks:
  pangolin:
    external: true
  backend:
