services:
  ollama:
    networks:
      - frontend
  open-webui:
    networks:
      - frontend
      - backend
  searxng-ai:
    networks:
      - backend
  tika:
    networks:
      -backend

networks:
  frontend:
    external: true
  backend: