services:
  ollama:
    networks:
      - ai
      - frontend
  open-webui:
    networks:
      - frontend
      - backend
  searxng-ai:
    networks:
      - backend
  tika:
    networks:
      -backend

networks:
  ai:
    external: true
  frontend:
    external: true
  backend: