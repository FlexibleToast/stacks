services:
  adguardhome:
    networks:
      adguard-net:
        ipv4_address: ${ADGUARD_HOME_IP}
  adguardhome-sync:
    networks:
      adguard-net:
        ipv4_address: ${ADGUARD_SYNC_IP}

networks:
  eth0:
    external: true
