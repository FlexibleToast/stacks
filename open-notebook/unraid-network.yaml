services:
  on-speaches:
    networks:
      - ai
  on-surrealdb:
    networks:
      - backend
  on-open-notebook:
    networks:
      - ai
      - backend
      - frontend

networks:
  ai:
    external: true
  backend:
  frontend:
    external: true
