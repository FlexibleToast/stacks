services:
  on-speaches:
    networks:
      - backend
  on-surrealdb:
    networks:
      - backend
  on-open-notebook:
    networks:
      - ai
      - backend
      - frontend

networks:
  ai:
    external: true
  backend:
  frontend:
    external: true
